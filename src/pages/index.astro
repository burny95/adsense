---
// 홈 페이지
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import { SITE_NAME, SITE_DESCRIPTION, CATEGORIES } from '../consts';
import { TOOLS } from '../tools';

const recentPosts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime())
  .slice(0, 5);
---

<Layout
  description="CSS 값 계산, 색 대비 검사, 포맷 변환 같은 작업을 브라우저에서 바로. 웹 퍼블리셔·프론트엔드 개발자를 위한 무료 도구와 실무 가이드."
>
  <h1>웹 퍼블리셔를 위한 무료 프론트엔드 도구</h1>
  <p class="tool-intro">
    CSS clamp(), px ↔ rem, box-shadow, 색 대비(WCAG), OG 태그, 네이밍 컨벤션까지.
    설치도 로그인도 없이 브라우저에서 바로 씁니다.
  </p>

  <h2 class="section-label">도구</h2>
  <div class="card-grid">
    {
      TOOLS.map((tool) => (
        <a class="card" href={`/tools/${tool.slug}/`}>
          <h3>
            {tool.title} <span class="badge">{CATEGORIES[tool.category]}</span>
          </h3>
          <p>{tool.description}</p>
        </a>
      ))
    }
  </div>
  <p><a href="/tools/">도구 전체 보기 →</a></p>

  {
    recentPosts.length > 0 && (
      <>
        <h2 class="section-label">최근 글</h2>
        <ul class="stack-list">
          {recentPosts.map((post) => (
            <li>
              <a href={`/blog/${post.id}/`}>{post.data.title}</a>
              <p>{post.data.description}</p>
            </li>
          ))}
        </ul>
        <p>
          <a href="/blog/">블로그 전체 보기 →</a>
        </p>
      </>
    )
  }
</Layout>
